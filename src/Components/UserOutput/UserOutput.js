import React from 'react';

const UserOutput = (props) => {
    return (
        <div>
            MElOOOOOOO <b> {props.resultado ? 'si es pana' : 'noniferos es pri'}</b>
        </div>
    );
}

export default UserOutput;